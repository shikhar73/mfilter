version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install

  build:
    commands:
      - mkdir build
      - cp index.html build/

artifacts:
  files:
    - '**/*'
  base-directory: build